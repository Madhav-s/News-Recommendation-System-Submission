# Model Checkpoints

This folder can contain trained model checkpoint files (.pt).

To generate a checkpoint:
1. Run training with python src/train.py using your preferred arguments.
2. Save model state dict to this directory (for example models/final_model.pt).

Note: Checkpoints may be excluded from version control if file sizes are large.
